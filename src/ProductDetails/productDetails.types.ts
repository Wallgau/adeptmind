export interface ProductDetailsProps {
    product: {
        title: string,
        description: string,
        image: string
    }
}

export interface ProductDetailsStyleProps {
    selectedDisplay: string,
}